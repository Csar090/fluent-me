// Fluency OS public bootstrap configuration.
// Set backendUrl once to the permanent Apps Script /exec deployment.
// Future Apps Script updates must edit this same deployment so the URL remains stable.
window.FLUENCY_CONFIG = Object.freeze({
  backendUrl: ""
});
